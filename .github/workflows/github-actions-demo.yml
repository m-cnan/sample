name: Cache and Test Workflow

on:
  push:
    branches: [ "master" ]   # or "main"
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      # Checkout repository
      - name: Checkout repo
        uses: actions/checkout@v4

      # Cache dependencies
      - name: Cache dependencies
        id: cache-step
        uses: actions/cache@v4
        with:
          path: ~/.cache
          key: cache-b29f931

      # Prime cache step (multi-line syntax)
      - name: prime-cache-b29f931
        run: |
          echo "Cache result: ${{ steps.cache-step.outputs.cache-hit }}"

      # Step with your email in the name
      - name: 24f2001467@ds.study.iitm.ac.in
        run: |
          echo "Hello, world!"
